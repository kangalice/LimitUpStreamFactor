## 2025-05-22更新

1. 优化了框架性能。在基准测试下，3进程的速度相较于基准优化了40%(10min->6min)，使用9核时已打到IO上限。
2. 调整了API接口，统一了trade/order的结构体，同时将参数调整为const，在一遍输入使用过后，输入到spi的数据就会被销毁。
3. 增加了对象池，建议在需要保存历史数据时使用对象池+unordered map进行保存。

## 2025-06-03更新

1. 调整了demo的代码结构，完善了README文档，对代码书写进行了更详细的约定。

## 2025-06-04更新

1. 优化了上交所原始逐笔委托还原逻辑，调整了回调接口的数据输出逻辑，逐笔委托接口不再返回上交所逐笔成交对应的委托，需在逐笔成交接口处处理。

## 2025-06-12更新

1. 调整了trade处理函数中对于上交所集合竞价的处理，跳过side=N的逐笔成交还原。
2. README增加对于开发细节的说明。